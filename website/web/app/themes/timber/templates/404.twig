{% extends "_base/base.twig" %}

{% block content %}
  <div class="content-wrapper">
    <article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
      <section class="article-content">
        {% include 'blocks/hero_small.twig' with { block: {title: {size: 'h1', content: '404: Page not found'}, background:'bg-dark-grey'}}%}
        <div class="constrict-xs">
          <div class="article-body">
            <p>{{ __("Sorry, we couldn't find what you're looking for.", 'timber') }}</p>
          </div>
        </div>
      </section>
    </article>
  </div>
{% endblock %}
